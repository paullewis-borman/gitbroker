{
  "name": "gitbroker",
  "version": "1.0.0",
  "description": "Native git push service for sandboxed agents: run git add/commit/push in real working directories via a tiny secret-scoped HTTP API.",
  "type": "module",
  "bin": { "gitbroker": "./broker.mjs" },
  "scripts": {
    "start": "node broker.mjs"
  },
  "engines": { "node": ">=18" },
  "license": "MIT",
  "dependencies": {}
}
